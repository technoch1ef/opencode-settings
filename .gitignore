node_modules
dist

# Standardize on Bun; keep npm lockfiles out
package-lock.json

# local files
opencode-notifier-state.json

# Local MCP config (per-machine, not tracked)
opencode.local.json
opencode.backup.json

# Slack MCP cache
slack_mcp_users_cache.json
slack_mcp_channels_cache.json

# bv (beads viewer) local config and caches
.bv/
.beads/

# Private, local-only skills (never tracked)
skills-private/
