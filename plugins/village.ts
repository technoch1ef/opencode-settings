/**
 * Backwards-compatible re-export.
 *
 * The canonical source is now src/plugin.ts.
 * This file remains so the OpenCode plugin loader can still resolve it from plugins/.
 */
export { default } from "../src/plugin";
