/**
 * Tools barrel — placeholder for per-tool modules.
 *
 * Each tool (claim, scaffold, orphans, status, handoff, invoke, lint, board)
 * will be split into its own file in a subsequent bead (opencode-2c6.2).
 *
 * For now, all tools live inline in src/plugin.ts.
 */
